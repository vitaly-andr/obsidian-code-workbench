# Node / build (TypeScript Obsidian plugin)
node_modules/
dist/
build/
main.js
*.js.map

# Obsidian plugin local test vault
test-vault/

# Obsidian config for opening this repo as a vault (dev convenience, not part of the plugin)
.obsidian/

# Editor / agent local state (may hold credentials)
.claude/settings.local.json

# OS
.DS_Store
